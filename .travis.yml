language: cpp

matrix:
  include:
    - os: linux
      dist: bionic
      env: OS_PYTHON_VERSIONS=3.6

script:
  - sudo apt-get install -y cmake python3 python3-dev
  - pyenv global 3.7.1
  - cd build
  - cmake ..
  - cmake --build . -- -j2
